import React from "react";

// eslint-disable-next-line no-unused-vars
import Styles from './Footer.module.scss';

function Footer({toggleBasket}) {
	return (
    <footer>Footer</footer>
	);
}

export default Footer;
